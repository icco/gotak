---
extends: default
# Generated by `swag init` in a style yamllint's defaults don't like;
# yq would reformat them, which produces a loop with the swagger-docs
# workflow. Treat the docs/ directory as opaque output.
ignore: |
  cmd/server/docs/
rules:
  line-length: disable
  document-start: disable
  truthy: disable
